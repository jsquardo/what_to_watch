import React from 'react';

import Nav from '../components/Nav'

import MovieHeader from '../components/MovieHeader';

const Home = () => {
	return (
		<>
			<Nav />
			<MovieHeader />
		</>
	);
};

export default Home;
